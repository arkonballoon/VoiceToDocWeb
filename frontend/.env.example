# =============================================================================
# VoiceToDoc Frontend - Umgebungsvariablen Beispiel
# =============================================================================
# Kopieren Sie diese Datei zu .env und passen Sie die Werte an
# ACHTUNG: .env ist in .gitignore und wird nicht committed!
# =============================================================================

# =============================================================================
# Backend-Verbindung
# =============================================================================
# Backend-URL ohne Protokoll (z.B. localhost:8000 oder api.yourdomain.com)
VITE_BACKEND_URL=localhost:8000
# HTTP-Protokoll: http (Entwicklung) oder https (Produktion)
VITE_PROTOCOL=http
# WebSocket-Protokoll: ws (Entwicklung) oder wss (Produktion)
VITE_WS_PROTOCOL=ws

# =============================================================================
# WebSocket-Konfiguration
# =============================================================================
# Heartbeat-Intervall in Millisekunden (30000 = 30 Sekunden)
VITE_WS_HEARTBEAT_INTERVAL=30000
# Maximale Anzahl Wiederverbindungsversuche
VITE_WS_MAX_RECONNECT_ATTEMPTS=3
# VerzÃ¶gerung zwischen Wiederverbindungsversuchen in Millisekunden
VITE_WS_RECONNECT_DELAY=5000

# =============================================================================
# Audio-Konfiguration
# =============================================================================
# Maximale Upload-DateigrÃ¶ÃŸe in Bytes (52428800 = 50 MB)
VITE_MAX_FILE_SIZE=52428800
# Audio-Chunk-GrÃ¶ÃŸe in Millisekunden (fÃ¼r Aufnahme)
VITE_AUDIO_CHUNK_SIZE=5000
# Audio-Sample-Rate in Hz
VITE_AUDIO_SAMPLE_RATE=16000

# =============================================================================
# UI-Konfiguration
# =============================================================================
# Theme: light oder dark
VITE_THEME=light
# Sprache: de, en
VITE_LANGUAGE=de
# Auto-Save-Intervall fÃ¼r Editor in Millisekunden (30000 = 30 Sekunden)
VITE_AUTO_SAVE_INTERVAL=30000
