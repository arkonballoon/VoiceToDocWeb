# =============================================================================
# VoiceToDoc Backend - Umgebungsvariablen Beispiel
# =============================================================================
# Kopieren Sie diese Datei zu .env und passen Sie die Werte an
# ACHTUNG: .env ist in .gitignore und wird nicht committed!
# =============================================================================

# =============================================================================
# API-SchlÃ¼ssel (ERFORDERLICH)
# =============================================================================
# Ihr OpenAI API-SchlÃ¼ssel fÃ¼r LLM-basierte Template-Verarbeitung
LLM_API_KEY=your_openai_api_key_here

# =============================================================================
# CORS-Konfiguration
# =============================================================================
# Erlaubte Origins fÃ¼r Cross-Origin-Requests (JSON-Array Format)
# Entwicklung: localhost und Docker-interne Netzwerke
ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000","http://frontend:3000"]
# Produktion: Ihre Domain(s)
# ALLOWED_ORIGINS=["https://yourdomain.com","https://www.yourdomain.com"]

# =============================================================================
# Datenbank-Konfiguration
# =============================================================================
# Datenbanktyp: sqlite, postgresql
DB_TYPE=sqlite
# Pfad zur SQLite-Datenbank (relativ zum backend Verzeichnis)
DB_PATH=data/templates.db

# =============================================================================
# Logging-Konfiguration
# =============================================================================
# Log-Level: 10=DEBUG, 20=INFO, 30=WARNING, 40=ERROR, 50=CRITICAL
LOG_LEVEL=20
# SQL-Debug-Ausgabe aktivieren (true/false)
SQL_DEBUG=false

# =============================================================================
# Audio-Verarbeitung
# =============================================================================
# Minimale LÃ¤nge von Stille in Millisekunden
AUDIO_MIN_SILENCE_LEN=500
# Schwellenwert fÃ¼r Stille in dBFS (negativer Wert)
AUDIO_SILENCE_THRESH=-32
# Minimale LÃ¤nge eines Audio-Chunks in Millisekunden
AUDIO_MIN_CHUNK_LENGTH=2000
# Maximale LÃ¤nge eines Audio-Chunks in Millisekunden
AUDIO_MAX_CHUNK_LENGTH=5000

# =============================================================================
# Whisper-Konfiguration (Speech-to-Text)
# =============================================================================
# Whisper-Modell fÃ¼r CPU: tiny, base, small, medium, large
WHISPER_MODEL=base
# Whisper-Modell fÃ¼r CUDA/GPU: base, small, medium, large-v2, large-v3
WHISPER_DEVICE_CUDA=large-v3
# Maximale Anzahl paralleler Worker fÃ¼r Transkription
MAX_WORKERS=3
